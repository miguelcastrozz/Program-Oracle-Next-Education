export class Client {

    nameClient;
    dniClient;
    rutClient;

}

/*
Diferencias entre undefined y null:
    * Undefined es un valor que se asigna a una variable cuando no se le ha asignado ningún valor (let x)
    * Null es un valor que se asigna a una variable cuando se le ha asignado un valor nulo (let x = null)
*/
